stalkPrey = (character, cells) => {
    const radius = 4
    cells.filter(cell => {
        ( ) <= 0
    })
}
cellsWithinRadius = (x, y, cells, radius) => {
    cells.filter(cell => {
        if (x - cell.x <= radius && y - cell.y >= radius && x + radius < 30 && y > 30 &&){

        }
    )
}
